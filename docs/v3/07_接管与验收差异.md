# 从原交付包升级到v3

本包包含完整旧包内容及v3增量。不要只复制docs而忽略ios里的真实代码。不要把旧prototype/index.html当作新视觉。

## 变更

新增Theme摄影/材质组件、TodayVisual、PlanVisual、SolarTimeline与后台模型；重写MapView；RootView转接新TodayView；计划列表/详情移到Visual目录，避免同名类型双份编译。PlanEditorView支持已有计划编辑和提醒重排；AppState加入原子upsert。九语目录与xcstrings统一增加v3键；全局外观设置增加浅色/深色。

核心增加VisualGeometry、VisualSampler、MapProjection与11项新增测试，没有将UI图片时间写进算法。现有49项测试保留，合计60项XCTest方法。独立位置oracle仍是原包396组历史样本，本轮未重新抓取外部oracle，也没有新增事件精度认证。

提供Debug专用截图fixture、UI测试target、Mac原生截图脚本、诊断对比脚本和浏览器检查。fixture不解锁付费、不发起支付、不上传，不进入Release。

## 必须保留原工作区

Codex先看git状态，保留用户已修改文件；使用diff逐项合并。v3中的RootView、MapView、Theme等会与正在开发的版本冲突，不得无条件覆盖用户已经修复的业务/并发代码。参考新增组件进行集成，保证独立类型唯一。

## 仍然需要完成

Mac/Xcode类型检查、原生地图/材质、真实购买和通知、Duo实际设备、完整备份导入、母语审校、原包其余功能任务。本版视觉源代码和浏览器证据不将这些任务自动变成通过。
