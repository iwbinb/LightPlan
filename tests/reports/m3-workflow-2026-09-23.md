# M3 核心操作流程 — 2026-09-23

## 范围

基于已合并的 PR #3 / `0ace4241231785c47af00a5f2d89fc0a7281e192`。
本阶段改进机位/主体/搜索/预演/保存流程，不改星历模型、精度阈值、备份 schema、付费下载、签名或 main。

- 构图面板前置机位、主体与搜索入口，明确当前要预演和保存的时刻。修复手动地图时间被保存动作换成当天最佳时刻的问题；最佳值必须显式选择。
- 新增有界、仅内存的按天体搜索输入记忆，保留已应用日期、范围、条件、排序；重新进入重算结果，不复用旧几何结果。地图上下文变化重新定位起始日；模板/保存计划优先；不写入坐标偏好或修改备份格式。
- 无结果可直接调整条件或明确选择全部候选；计算错误提供重试/调整；搜索可停止并按原输入重启，排序不重启搜索。
- 编辑器冻结机位，校验预览请求身份，保存按钮不接受过时预览；脏草稿退出需确认，旋转或嵌套页面返回不重新初始化，失败保留输入。
- 新文案 14 个键 × 9 语言；没有删除旧测试、降低精度或加入 IAP/订阅/后台。

## 本地执行

隔离目录从只读 Actions 源码快照建立，以 Git blob 校验完整已合并源码。不是用户 Mac 工作副本。

- Linux x86_64 / Swift 6.2.1，完整核心 `swift test --package-path packages/LightPlanCore`：224 项，0 失败（原有 212 + 新增 12）。2026-09-23 15:53:59 UTC 完成。
- `PlanningSearchMemoryTests` 新增 12 项涵盖应用/取消、All、排序、日月隔离、结果选择、时区/日期/机位变更、显式覆盖、错误输入不污染已有设置。
- CI 脚本 36 自测通过；九语完整性 384 键 / 3456 译文、零缺失。自动完整性不等于母语审校。
- Swift UI 源码及测试的语法解析通过；Linux 不提供 SwiftUI/UIKit，解析不是 Apple 平台类型检查或模拟器执行。

## 原生验证

新增独立 `iphone-workflow` 分组（4 项原生任务），保留 M1 八组及完整清单审计。
覆盖手动时刻→预演→保存、草稿取消/旋转/明确放弃、无结果恢复与搜索重开、90 日搜索停止/重启。
当前记录为提交前候选；Apple 原生构建和测试结果必须根据此版本 Actions 实际产物补充，不能用 M1/M2 通过代签。

复现：

```sh
swift test --package-path packages/LightPlanCore
python3 scripts/ci_baseline_tests.py
python3 scripts/localization_audit.py
bash scripts/ci_native.sh checks
bash scripts/ci_native.sh iphone-workflow
```

M4 未开始。真机通知/地图/Widget、最低系统、母语、VoiceOver、分发签名与商店验收依旧独立。
