# M2 — 计算与搜索可靠性（2026-09-23）

## 当前状态

M2 计算与搜索核心实现已完成，macOS Actions 完整核心回归 **212/212 通过，0 失败**。同一源码的原生 checks（含 Debug 构建、未签名 Release 归档）也通过。UI 分组另行核验，M1 已有 UI 失败没有因此关闭；不声明全 App、真机或商店验收通过。

基线为 dev 的 `23decd3baf5180dffb888b6e41e86b59cd0d4541`。仅修改计算核心、增加回归测试及独立快速核心工作流；不改 UI、素材、九语、备份格式、天文系数、收费、签名或 main。实现提交：`99326e35cac863e490d417a3d92ecb47f4c7b292`。本次证据补充提交只修改文档，不改变已测源码。

## 修正及边界

1. **整日首尾极值漏采样**：原 DayEngine 只在有左右邻点的内部格点检测极值。太阳或月亮在午夜附近极短暂穿越阈值时，首/尾格点均在阈值外会遗漏事件。新增共享 SearchSampling，显式精化首尾真实网格单元及内部极值，所有计算仍在请求区间内。整日 60/120 秒基础网格、0.02 秒极值搜索及 0.1 秒事件求根容差不变。
2. **搜索输入和结果保护**：零、负、非有限或不能推进 Date 的步长直接报错；非有限模型数值不能被默认为“没有匹配”。构造角差区间前验证容差，避免反向 ClosedRange 触发前置条件崩溃。区间搜索与事件计算共用经测试的采样规则，末尾不足一个步长时使用真实末格。
3. **取消响应**：每次采样/极值精化以及事件求根循环检查取消；不返回被取消任务的部分结果。不改变原有上层请求隔离架构。
4. **常量函数**：避免对每个平坦内部格点反复做极值搜索。只保留必要端点探测；不是 iPhone 性能或耗电基准。

这是一轮数值求解与契约可靠性修复，不是新增或更换星历模型，不声称改善实际天文精度、地形可见性或保证拍摄成功。太阳/月亮位置和升落的独立参考阈值保持原值，冻结参考用例保留。

## 新增 25 项 XCTest 回归

- SearchSamplingTests：12 项；首/尾/内部极值、短末格、小于一步的区间、无越界求值、日期去重、平坦函数、异常步长/数值和取消。
- CalculationSearchReliabilityTests：13 项；真实 DayEngine 午夜前后短黄金窗口、搜索输入错误、零宽条件、短末格窗口边界、日月并发与取消后新搜索，以及目的地日历边界。
- 日历矩阵同时覆盖日/月：纽约 23/25 小时、Lord Howe 23.5/24.5 小时、Apia 跳过日期、Kwajalein 历史 47 小时民用日、Kathmandu 四分之一小时时区、1900/2100 两端及极端时区。
- 午夜短窗口用同一太阳位置模型构造合法手动地点，并用独立测试求极值过程产生反例。它证明求解器不会漏掉这个模型内的短窗口，**不是外部星历精度参考**。
- 既有位置、升落、焦段、窗口、计划及备份测试保持不变；没有删测试或放宽精度。

## 本地已执行

环境：Linux x86_64，Swift 6.2.1，无 Xcode。网络克隆不可用，读取固定提交的文件后，以 Git blob SHA 核对两个原始修改文件；使用独立临时目录，不接触用户工作副本。

| 验证 | 结果 | 范围 |
|---|---|---|
| `swift test`，独立采样测试包 | 12 项通过，0 失败 | 实际 SearchSampling 及原有错误枚举，不含 App/星历 |
| `swift test`，补充生产 scalarIntervals 原样切片 | 16 项通过，0 失败 | 前述 12 项加 4 个真实标量窗口用例；不等于完整核心测试 |
| 新增集成测试 Swift 语法检查 | 退出 0 | 尚不能代替类型检查或执行 |

本地切片使用生产方法原样提取，没有用模拟天文结果代替真实集成测试。完整回归改由 macOS Actions 在完整仓库上执行。

## Actions 与已知基线问题

- 保留 M1 原有七组原生 CI；新增 `Core calculation regression`，在相同 Xcode 26.3 环境直接运行完整 Swift 核心套件并输出完整日志。这个快速检查不能替代 Debug/Release/模拟器或真机验收。
- M1 运行 `35841204831` 的 checks 已成功，但 iphone-product、iphone-planning 返回 xcodebuild 65（内部包装器未超时）。规划日志有 `opportunity-days-presets`、`plan-notes` 无法触达的附件记录；截图/结果包已上传，具体产品与测试交互原因仍需逐项定位。同次 iphone-visual 成功；M2 提交触发新工作流后，原有并发取消规则终止其余三组，已保留产物。新提交运行 `35843822133` 完整重跑七组，不用旧结果代签新版本。
- 这些失败属于 M2 源码提交前的基线，不因本次核心修正自动关闭，也不删除失败证据。不要把 M1 或全 App 宣称全绿。

复现：

```sh
swift test --package-path packages/LightPlanCore --filter 'SearchSamplingTests|CalculationSearchReliabilityTests'
swift test --package-path packages/LightPlanCore
bash scripts/ci_native.sh checks
```

M2 完整核心回归及本轮源码的原生 checks 已通过。真实现场精度、通知送达、VoiceOver、真机地图、正式签名与商店提交仍是独立门槛。

## 本轮完整核心执行证据

- 工作流：`Core calculation regression`，运行 `35843822247`，任务 `107125023850`，commit `99326e35cac863e490d417a3d92ecb47f4c7b292`。
- 环境：macOS arm64、Xcode 26.3 (17C529)、Apple Swift 6.2.4。
- `swift test --package-path packages/LightPlanCore`：**212 项通过，0 失败**；2026-09-23 09:36:48.847 UTC 完成。新增采样 12 项与集成可靠性 13 项全部通过，既有 187 项保留并通过。
- 冻结独立位置参考、独立升落事件参考、12 项焦段构图测试、13 项搜索与 15 项窗口测试均实际执行通过。没有修改参考样本、阈值或旧断言。
- 原始证据 artifact `10742557344`，ZIP SHA-256 `b316a0a2c760b1899e01987d836fe6a63c108e29b24d068d1ca89f6d74a03837`。结构化摘要见 `m2-core-verification-2026-09-23.json`。

完整核心通过不等于原生界面或实体设备通过。后续应先定位 M1 留下的控件可达性/测试交互失败，再进行 M3 的流程优化；本轮没有开始 M3。

## 本轮原生构建执行证据

- Native run `35843822133` 的 checks 任务 `107125573481` 于 2026-09-23 09:40:49 UTC 成功结束，源码仍为 `99326e35cac863e490d417a3d92ecb47f4c7b292`。包装器记录退出 0、未超时、未中断，耗时 212.161 秒。
- 此分组按已核对的脚本顺序执行 CI 自测、本地化与 metadata 审计、发布工具与 schema 回归、工程生成一致性、完整核心测试、Debug 模拟器构建及未签名 Release 归档。任一步失败均返回非零；本次整个分组成功。
- 证据 artifact `10741769940`，ZIP SHA-256 `6f927f30016aa1a75eed9a04bb5ff5070f0026de74dc931f4b333c8122f36916`，包含原始命令记录、日志及归档。
- 这是构建与工具回归验收，不是正式分发签名、商店发布预检通过或 UI 全绿。文档补充时 UI 组仍执行/排队，不修改其实际状态。
