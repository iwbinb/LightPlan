# M3 核心操作流程 — 2026-09-23

## 当前验收状态（2026-09-24 补齐）

**M3 开发及本阶段自动化验收完成；可作为 `dev → main` 合并候选。不是 App Store 发布完成。**

验收源码：`76a3a3b04768c58efdb35be34d60a23bb107ea5f`；源码树：`19f3945e92a560a665822e49b148965dcec73854`。基线是已合并 PR #3 的 `0ace4241231785c47af00a5f2d89fc0a7281e192`。

[Native app verification #52](https://github.com/iwbinb/LightPlan/actions/runs/35892933684)，运行 `35892933684`，attempt 1，2026-09-23 17:58:56 UTC 完成（北京时间 2026-09-24 01:58:56）：**9/9 组成功**。本次补文档读取的是已完成的最终源码产物，没有再次执行 App，也没有改写旧失败。PR 合并引用的检查另行执行，不能用本次 dev-source 结果代签。

## 功能与验收对应

| 范围 | 实现与证据 | 验收边界 |
|---|---|---|
| 机位 → 主体 → 查找窗口 → 预演 → 保存 | 构图面板前置主要入口；当天最佳时刻必须主动选择。新增 `WorkflowUITests/testManualTimelineTimeIsTheTimePreviewedAndSaved` 比较地图手动步进后的时刻、预演时刻和计划编辑器锚点 | 该新增用例到编辑器后取消，不单独证明手动时刻写盘再重启；实际保存/恢复由现有产品与规划用例另外覆盖，不把两种证据混为一个测试 |
| 保留已应用的搜索输入 | `PlanningSearchMemoryTests` 12 项；原生 `testEmptySearchRecoveryAndAppliedOptionsSurviveReopening` 验证取消选项不改条件、清除条件、30 日范围、排序、跨 Tab 重开 | 仅当前应用会话；太阳/月亮各一份。重新进入重算结果，不复用旧几何结果；退出进程后的恢复不在本阶段承诺内 |
| 搜索错误与恢复 | 无匹配可调整条件或由用户明确清除；计算错误有重试/调整；原生用例验证两个恢复按钮独立 | 空结果不是错误，清除条件不能自动触发；没有把天气、遮挡或机位可达性当作搜索保证。错误提示的所有环境故障组合未逐项真机注入 |
| 停止与重新搜索 | `testStoppedSearchRetainsOptionsAndCanRestart` 验证 90 日搜索停止、参数保留、取消选项和重新搜索完成 | 是按原参数重新计算，不是保存部分结果或后台断点续算 |
| 未保存编辑保护 | `testUnsavedEditorSurvivesCancelAndRotationUntilExplicitDiscard` 验证继续编辑、标题/项目保留、横竖屏和明确放弃后不写入计划；编辑器冻结机位并隔离过时计算预览 | 不承诺进程被系统终止后恢复未保存草稿；保存失败/存储损坏及完整数据可靠性仍按 M4 和发布门槛验证 |
| 既有能力无回退 | 产品 9 项、规划 6 项及完整核心回归均通过，覆盖已有保存/编辑/重启/地图恢复、提醒、日月模板和时间轴 | 模拟器 OS 队列检查不代表真机锁屏送达；有限测试不等于所有场景证明 |
| 九语言与布局 | 14 个新增键覆盖全部九语言；完整目录 384 键、3456 译文。既有九语、大字号和 iPad 旋转组保留且通过 | 自动完整性和可达性不是母语、VoiceOver 或首次用户可用性研究，也不是最终视觉验收 |

以上方法均位于 `ios/LightPlanVisualUITests/`；核心输入记忆测试位于 `packages/LightPlanCore/Tests/LightPlanCoreTests/PlanningSearchMemoryTests.swift`。完整选择器与执行计数见 [结构化证据清单](m3-native-verification-2026-09-24.json)。

搜索上下文规则与 [产品流程规范](../../docs/02_全流程与状态.md) 一致：改机位/主体/偏移或主动换地图日期时重新锚定起始日，保留该天体的条件和日数；选择搜索结果不改变原完整范围；显式模板和保存计划优先；排序不启动新的天文搜索。

## 最终源码的实际执行结果

| 检查组 | 结果 | 原始 artifact ID |
|---|---|---|
| checks | 224 核心、36 CI 自测、Debug、未签名 Release 与工具回归通过 | 10766075854 |
| iPhone 产品流程 | 9/9 | 10766112754 |
| iPhone 规划流程 | 6/6 | 10767297911 |
| M3 专项流程 | 4/4 | 10766402951 |
| iPhone 基础视觉 | 5/5 | 10766713919 |
| iPhone 扩展视觉 | 3/3，含 1 项既有滚动预算辅助逻辑测试 | 10767492092 |
| iPhone 扩展最大字号 | 德语、泰语 2/2 | 10768390337 |
| iPhone 基础最大字号 | 德语、泰语 2/2 | 10767607144 |
| iPad 旋转 | 1/1 | 10767892317 |

原生目标共 **32 次执行：31 次真实 UI 执行 + 1 项辅助逻辑回归**。iPhone 的 31 个不同方法完整执行一次，iPad 再执行既有旋转方法。所有原生结果包中失败、跳过和预期失败均为零；全部所选方法与 `test-inventory.json` 一致，没有遗漏或重复计入 iPhone 清单。

环境：macOS arm64、Xcode 26.3 (17C529)、Apple Swift 6.2.4、iOS 26.2 Simulator；iPhone 17 Pro 和 iPad Pro 11-inch (M4)。`checks` 日志另确认：发布预检工具单测 24/24、截图打包工具 10/10、schema 15/15；九語 metadata 审计通过；工程生成无漂移；`BUILD SUCCEEDED`、`ARCHIVE SUCCEEDED`；归档未含 StoreKit fixture。工具单测通过不等于真实账号发布预检 ready，未签名归档不是分发签名。

2026-09-24 已逐一读取九份 ZIP，核对 SHA-256、`source-commit.txt`、`runner.json`、`commands.tsv`；UI 组同时核对 `selected-tests.txt`、`test-summary.json` 和清单覆盖。所有阶段退出码为 0，无超时或中断。哈希、任务 ID、选择器和产物链接保存在同名结构化清单。产物按当前工作流保留 14 天，预计于 2026-10-07 到期；本报告只记录引用和摘要，不把过期下载链接当作永久备份，也不将 xcresult 全量提交进 Git。

## 已发现的问题与修复记录

- `95ab9ce`：M3 初始实现，新增 12 项核心测试、4 项原生流程与 14 个九语键。
- run 50 的原生证据暴露三个交互问题：List 默认按钮样式使“调整/清除”同时触发；停止状态容器的标识影响重新搜索按钮；退出确认弹出层没有显示取消角色的“继续编辑”。`4df5c5f` 分别使用独立按钮样式、缩小状态标识范围，以及同时展示保留/放弃的 alert 修复，保留并加强原测试。手动时刻传递用例在该轮已通过，其余失败没有删除或标为跳过。
- run 51 后的截图审查发现复用搜索入口仍宣称“未来 14 天”，与已应用的 30/90 日范围不符。`76a3a3b` 将入口统一为“查找拍摄窗口 / Find shooting windows”，同步九语言并增加原生精确文案断言。run 51 的局部通过不作为最终修订验收，最终以 run 52 全部九组结果为准。
- 最终树已移除临时源码传输工作流；本次交接仅补文档，不增加具有写权限的诊断任务，不改核心模型、精度、存储格式、付费下载、签名或 main。

## 原生截图抽查与未关闭门槛

本次查看 run 52 的七张 M3 流程截图，确认 30 日范围与 Date 排序重开一致、手动预演与编辑器均为 3:17 PM、停止状态及重启后 90 日实际范围，以及草稿内容仍在。另抽查德/泰最大字号三类页面和 iPad 横竖屏地图。

这些图片不是最终商店素材：草稿截图含系统键盘引导浮层；最大字号存在长导航标题省略、长文本换行，以及 sheet 后方内容的视觉干扰。记录为后续 M5 视觉与素材处理范围，不声称全应用零裁切。九语自动截图通过不等于每屏已由母语用户逐项审校。

仍独立保留：首次摄影用户独立完成任务的可用性验证；M4 的数据、备份与外拍可靠性；最低支持系统、实际 iPhone/iPad 和可用硬件覆盖；VoiceOver；真机通知、GPS/地图、Widget；性能/耗电；M5 最终视觉；M6 分发签名、品牌、网站、App Store Connect 账号与提交验收。M4 未开始，未合并 main、未自动合并、未发布。

## 复现

在配置好的 Mac 上，检出上述验收源码后执行（不是本次文档任务重新执行的命令）：

```sh
swift test --package-path packages/LightPlanCore
python3 scripts/ci_baseline_tests.py
python3 scripts/localization_audit.py
bash scripts/ci_native.sh checks
bash scripts/ci_native.sh iphone-workflow
bash scripts/ci_native.sh all
```

完整原生命令需要与工作流匹配的 Xcode、已安装的 iOS 模拟器和设备；不得用 Linux 语法检查替代 Apple 编译或 UI 运行。PR 创建后的合并引用检查单独以其 Actions 结果为准。

## 历史提交前本地记录（保留原范围）

此前隔离目录从只读 Actions 源码快照建立，并用 Git blob 校验已合并源码，不是用户 Mac 工作副本。原报告记录：Linux x86_64 / Swift 6.2.1 完整核心 224/224 于 2026-09-23 15:53:59 UTC 完成；CI 脚本 36 项通过；九语完整性 384 键 / 3456 译文、零缺失；Swift UI 源码及测试语法解析通过。当时 Apple 原生结果待补，现已由上方 run 52 最终证据替代其“待验收”状态。历史本地结果不被重新标记为本次执行，原 Git 历史保留。
